# Authentication --* not working..don't know why
1. install laravel/jetstream    ```composer require laravel/jetstream```
2. install livewire ``` php artisan jetstream:install livewire```
3. migrate database ```php artisan migrate```